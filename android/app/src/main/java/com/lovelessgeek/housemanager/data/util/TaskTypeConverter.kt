package com.lovelessgeek.housemanager.data.util

import androidx.room.TypeConverter
import com.lovelessgeek.housemanager.shared.models.TaskType

class TaskTypeConverter {
    /*
    @TypeConverter
    public fun toTaskType(taskType: String) = when (taskType.toUpperCase()) {
        "INSTANT" -> TaskType.INSTANT
        else -> TaskType.INSTANT
    }

    @TypeConverter
    public fun fromTaskType(taskType: TaskType) = when (taskType) {
        TaskType.INSTANT -> "INSTANT"
        else -> "NONE"
    }
    */
}